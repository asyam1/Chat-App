To start the application:

1) open a terminal and start mongodb
2) change directory in another terminal to the server directory and type npm install and then nodemon start 
3) change directory in another terminal to the client directory and type npm install and then npm run serve